{
  "name": "quotely-hardy",
  "version": "1.0.0",
  "description": "Inspire, Motivate, and Reflect with Quotely",
  "main": "index.html",
  "scripts": {
    "dev": "live-server . --port=3000",
    "build": "mkdir -p dist && cp -r *.html css/ js/ images/ dist/ 2>/dev/null || echo 'Building static site'",
    "deploy": "echo 'Deploy to Netlify via git push'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "nodemailer":"^6.9.9"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
